services:
  - type: web
    name: ytdown
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ytdown:app --timeout 300 --worker-class gthread --workers 1 --threads 8 --bind 0.0.0.0:10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
